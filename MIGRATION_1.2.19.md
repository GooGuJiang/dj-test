# 1.2.19 升级说明：分析完成后可靠自动重排

本版本不需要迁移缓存或配置。

## 行为变化

- 勾选“全部分析完成后自动重排未播放队列”后，导入歌曲触发的排序请求不会再因其他模型仍在运行而丢失。
- MuQ 被关闭或自动分析失败时，程序会使用 BPM、All-In-One 结构标签和已有 MuQ 缓存完成降级排序。
- 播放中重排仍保护已经播放的队列前缀；转场已经开始时，当前可听到的下一首也保持固定。
- 若算法判断原顺序已经最佳，状态栏会明确显示“当前顺序已是最优，无需移动”。
